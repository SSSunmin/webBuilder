# Supabase 백엔드 설정. 이 두 값이 모두 있으면 앱은 Supabase에 저장하고
# 로그인을 요구합니다. 비어 있으면 브라우저 localStorage로만 동작(인증 없음).
#
# 사용법: 이 파일을 `.env.local`로 복사한 뒤 Supabase 프로젝트 값으로 채우세요.
#   (Supabase 대시보드 → Project Settings → API)
# `.env.local`은 .gitignore에 의해 커밋되지 않습니다.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
