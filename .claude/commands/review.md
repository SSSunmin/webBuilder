---
description: 현재 변경분을 심각도별로 리뷰한다
---
현재 작업 트리의 변경분(또는 최근 수정한 코드)을 리뷰해줘.

- 버그·로직 결함·엣지케이스 누락을 우선 본다
- 심각도를 매긴다: critical(버그/보안) / major(설계 결함) / minor(스타일)
- 확실한 것 위주로 지적하고, 추측이면 표시한다
- 직접 고치지 말고 지적만 한다 (고치라고 하면 그때 고친다)

규모가 크면 `code-reviewer` 에이전트에 위임해도 좋다.
